<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>APE Cernier</title>
</head>
<body>
  <h1>Articles</h1>
  <ul>
    {% for post in collections.post | reverse %}
      <li>
        <a href="{{ post.url }}">{{ post.data.title }}</a> - {{ post.date | date('dd/MM/yyyy') }}
      </li>
    {% endfor %}
  </ul>
</body>
</html>
